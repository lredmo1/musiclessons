{
  "name": "musiclessons",
  "description": "By Lisa Primeaux-Redmond",
  "engines": {
    "node": "16.8.0",
    "npm": "8.0.0"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "clean": "rm -rf public",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "npm run clean && npm run build && npm run deploy"
  }
}